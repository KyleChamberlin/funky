# funky list

List all Funky-managed functions.

## Usage

```sh
funky list
```

## Output

Displays all functions stored in your Funky directory, listed by name.

```sh
$ funky list
deploy
hello
test-all
rebuild
```

Lists functions from the directory specified by `--funky-dir` (default: `~/.funky/`).
